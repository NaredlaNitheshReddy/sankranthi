package com.example.sankranthi

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Local unit test — runs on the JVM, no device needed.
 * `./gradlew test`
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
